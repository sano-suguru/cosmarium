/** mergeExp の構造的上限（★3 到達値） */
export const MAX_MERGE_EXP = 5;

/** mergeExp 1あたりの HP/Damage ブースト率 */
export const MERGE_STAT_BONUS = 0.04;

/** mergeExp 1あたりの生産時間短縮率 */
export const MERGE_PRODUCTION_BONUS = 0.03;
